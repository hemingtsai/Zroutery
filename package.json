{
  "name": "zroutery",
  "private": true,
  "version": "0.4.0",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:debug": "tauri build --debug",
    "ui:dev": "pnpm --dir ui dev",
    "ui:build": "pnpm --dir ui build",
    "test": "cargo test --workspace",
    "smoke": "cargo build -p zroutery --bin zroutery-headless && python3 scripts/smoke_test.py",
    "test:layout": "pnpm --dir ui build && python3 scripts/ui_layout_test.py --self-test && python3 scripts/ui_layout_test.py"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}